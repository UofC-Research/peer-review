[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "peer-elt"
version = "0.1.0"
description = "ELT, acquisition, and methodology pipeline for quantifying manuscript changes during peer review"
readme = "README.md"
requires-python = ">=3.13"
authors = [
    { name = "Albert Leung", email = "albert.leung@ucalgary.ca" }
]
dependencies = [
    "duckdb>=1.4.3",
  "pandas>=3.0.3",
  "pyarrow>=24.0.0",
    "pyyaml>=6.0.3",
  "requests>=2.32.3",
    "tenacity>=9.1.4",
    "sqlalchemy>=2.0.48",
  "psycopg2-binary>=2.9.9",
  "PyPDF2>=3.0.1",
]
keywords = [
    "preprints",
    "peer review",
    "bioRxiv",
    "medRxiv",
    "ELT",
    "research methods",
]

classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Science/Research",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.13",
  "Operating System :: OS Independent",
    "Topic :: Scientific/Engineering :: Information Analysis",
]

[project.urls]
Repository = "https://github.com/UofC-Research/peer-review"
Issues = "https://github.com/UofC-Research/peer-review/issues"
Homepage = "https://github.com/UofC-Research/peer-review"

[project.scripts]
peer-elt = "peer_elt.cli:main"

[project.optional-dependencies]
dev = [
  "pytest>=9.0.3",
  "pytest-cov",
  "black",
  "ruff",
]

[tool.setuptools]
package-dir = { "" = "src" }

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
addopts = "-ra -q"

[tool.black]
line-length = 88
target-version = ["py313"]

[tool.ruff]
line-length = 88

[tool.ruff.lint]
select = ["E", "F", "I"]
